# Maintainer: Antoine Stevan
pkgname=amtoine-scripts-git
pkgver=r34.64521f4
pkgrel=1
epoch=
pkgdesc="My personal collection of scripts."
arch=(x86_64)
host="github.com"
user="amtoine"
repo="scripts"
url="https://$host/$user/$repo.git"
license=('GNU')
groups=()
depends=()
makedepends=(git)
checkdepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
install=
changelog=
source=("git+$url")
noextract=()
md5sums=('SKIP')
validpgpkeys=()

pkgver() {
    cd "${_pkgname}"
    printf "r%s.%s" "$(git -C "$repo" rev-list --count HEAD)" "$(git -C "$repo" rev-parse --short HEAD)"
}

package() {
    cd "$repo"
    mkdir -p ${pkgdir}/usr/local/bin/
    mkdir -p ${pkgdir}/usr/local/share/amtoine-scripts/examples/
    cp -rf scripts/* ${pkgdir}/usr/local/bin/
    cp -rf examples/* ${pkgdir}/usr/local/share/amtoine-scripts/examples/
}
